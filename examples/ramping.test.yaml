# yaml-language-server: $schema=../schema/test.schema.json
#
# Run with: perfscale run -f examples/ramping.test.yaml -c examples/ramping.config.yaml
# (also pairs with spike.config.yaml and arrival-rate.config.yaml)
steps:
  - name: homepage
    use: std/http@v1
    with:
      method: GET
      url: https://httpbin.org/get
    check:
      status: 200
    outputs: resp

  - name: log status
    use: std/log@v1
    with:
      message: "got status ${{ resp.status }}"

  - use: std/sleep@v1
    with:
      ms: 200
